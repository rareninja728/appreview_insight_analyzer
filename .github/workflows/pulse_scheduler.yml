name: INDmoney Weekly Pulse Scheduler

on:
  schedule:
    # Runs every 5 minutes as requested. 
    # Runs daily at 09:50 UTC (3:20 PM IST)
    - cron: '50 9 * * *'
  # Allows you to trigger the workflow manually from the GitHub Actions tab
  workflow_dispatch:

jobs:
  execute-weekly-pulse:
    runs-on: ubuntu-latest

    env:
  EMAIL_ADDRESS: ${{ sugandhawankar123@gmail.com }}
  EMAIL_APP_PASSWORD: ${{ lprq rnex tmff afcf }}
  GROQ_API_KEY: ${{ ${{ secrets.GROQ_API_KEY }}}}
  GEMINI_API_KEY: ${{ AIzaSyDbFC90GC62tgDHGTqV8A7NPR_4ScYvAqI }}
      
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: Run Weekly Pulse Pipeline
        env:
          # These secrets must be added to your GitHub Repository Settings -> Secrets and variables -> Actions
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          EMAIL_ADDRESS: ${{ secrets.EMAIL_ADDRESS }}
          EMAIL_APP_PASSWORD: ${{ secrets.EMAIL_APP_PASSWORD }}
        run: |
          # This command executes the full pipeline and sends the email
          python cli.py run
